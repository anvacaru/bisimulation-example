### Bisimulation Example in Python using linked lists

Building:
```sh
poetry install
```

Run with
```sh
poetry run bisimulation-example
```

Run code checks
```
make format; make
```
